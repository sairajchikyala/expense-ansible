- name: frontend setup
  hosts: all
  become: yes
  tasks:
    - name: install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest

  # frontend setup
  # frontend setup


  # frontend setup
  # frontend setup

