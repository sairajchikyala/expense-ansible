- name: frontend setup
  hosts: all
  become: yes
  tasks:
    - name: install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest


    - name: Start Nginx
      ansible.builtin.systemd_service:
       state: restarted
       name: nginx


  # frontend setup
  # frontend setup

